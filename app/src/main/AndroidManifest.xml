<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.raras.testing"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".TestApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".presentation.views.activity.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.views.activity.PostsListActivity"
            android:label="@string/title_activity_posts"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".presentation.views.activity.PostDetailActivity">
        </activity>
    </application>

</manifest>
